<svg width="120" height="120" viewBox="0 0 120 120" xmlns="http://www.w3.org/2000/svg">
  <style>
    .bg { fill: #3456E8; }
    .text { fill: white; font-family: sans-serif; font-size: 24px; text-anchor: middle; dominant-baseline: central; }
  </style>

  <!-- Top left (a) -->
  <rect x="5" y="5" width="50" height="50" rx="10" class="bg" />
  <text x="30" y="30" class="text">a</text>

  <!-- Top right (circle) -->
  <circle cx="90" cy="30" r="25" class="bg" />

  <!-- Bottom left (Chinese character) -->
  <rect x="5" y="65" width="50" height="50" rx="10" class="bg" />
  <text x="30" y="90" class="text">bf6</text>

  <!-- Bottom right (smiley face) -->
  <rect x="65" y="65" width="50" height="50" rx="10" class="bg" />
  <text x="90" y="90" class="text">83d
de0a</text>
</svg>
